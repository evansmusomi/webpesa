<nav class="navbar navbar-inverse navbar-fixed-top">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <% if user_signed_in? %>
        <%= link_to 'WPESA', authenticated_root_path, class: 'navbar-brand' %> 
      <% else %>
        <%= link_to 'WPESA', unauthenticated_root_path, class: 'navbar-brand' %>
      <% end %>
    </div>
    <div id="navbar" class="navbar-collapse collapse">
      <ul class="nav navbar-nav">
      	<% if user_signed_in? %>
      		<%= nav_link innocent('<i class="glyphicon glyphicon-home"></i> Home'), authenticated_root_path %>
        	<%= nav_link innocent('<i class="glyphicon glyphicon-plus"></i> Top Up'), new_top_up_transactions_path %>
        	<%= nav_link innocent('<i class="glyphicon glyphicon-send"></i> Send Money'), new_transaction_path %>
        	<%= nav_link innocent('<i class="glyphicon glyphicon-user"></i> Profile'), edit_user_registration_path %>
          <li><%= link_to innocent('<i class="glyphicon glyphicon-log-out"></i> Log out'), destroy_user_session_path, method: :delete %></li>
      	<% else %>
      		<%= nav_link innocent('<i class="glyphicon glyphicon-home"></i> Home'), unauthenticated_root_path %>
	        <%= nav_link innocent('<i class="glyphicon glyphicon-user"></i> Register'), new_user_registration_path %>
      	<% end %>
      </ul>
    </div><!--/.nav-collapse -->
  </div>
</nav>